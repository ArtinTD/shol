package kon.shol;import org.apache.log4j.Logger;import java.io.IOException;import java.util.LinkedList;import java.util.concurrent.ArrayBlockingQueue;import java.util.concurrent.ExecutorService;import java.util.concurrent.Executors;public class Main {    static HBase hBase;    final static Logger logger = Logger.getLogger(kon.shol.Main.class);    static ArrayBlockingQueue<String> queue = new ArrayBlockingQueue<>(1000);    public static void main(String[] args) throws InterruptedException {        logger.error("NEW SESSION");        LinkedList<KafkaBaseCrawler> crawlers = new LinkedList<>();        ExecutorService executor = Executors.newFixedThreadPool(102);        Monitor monitor = new Monitor();        Consumer consumer = new Consumer("0", "sajjad");        Producer producer = new Producer("sajjad");        try {            hBase = new HBase("188.165.230.122:2181", "db");        } catch (IOException e) {            logger.error("Couldn't connect to HBase!");        }        //TODO set config for number of threads        executor.execute(consumer);        for (int i = 0; i < 100; i++) {            crawlers.add(new KafkaBaseCrawler());        }        monitor.setCrawlers(crawlers);        executor.execute(monitor);        for (KafkaBaseCrawler crawler : crawlers) {            executor.execute(crawler);        }    }}